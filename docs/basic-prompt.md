Crie uma API REST simples em Node.js.

Requisitos:
- Usar Node.js + Express
- Estrutura organizada por pastas
- CRUD completo de usuários
- Dados podem ficar em memória (sem banco)
- Validação básica dos dados
- Tratamento de erros
- Uso de variáveis de ambiente com `.env`
- Scripts de desenvolvimento no `package.json`
- Código limpo e moderno
- Respostas JSON padronizadas
- Comentários apenas quando realmente necessário

Rotas:
- GET /users
- GET /users/:id
- POST /users
- PUT /users/:id
- DELETE /users/:id

Extras:
- Usar UUID para IDs
- Middleware global de erros
- Middleware de logs
- Separar routes, controllers, services e middlewares
- Criar README simples explicando como rodar

No final:
- Mostrar estrutura de pastas
- Mostrar comandos para instalar e executar
- Garantir que o projeto rode sem erros